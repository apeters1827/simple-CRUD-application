mixin flashMessage()
  - const msg = flash.get();
  if (msg)
    - const { text, type } = msg;
    .alert.alert-dismissible.fade.show.m-3(role='alert' class=`alert-${type}`)= text
      button.close(type='button' data-dismiss='alert')
        span(aria-hidden='true') &times;